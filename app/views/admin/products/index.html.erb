
<h3>Products</h3>

<div class="card">
  <div class="card-body">
    <h5  class="card-title"> Index </h5>
    <%= link_to "New", admin_new_product_path, class: 'btn btn-primary' %>
    <% if flash[:set_product_error] %>
      <%= flash[:set_product_error] %>
    <% end %>

    <table class="table table-hover">
      <tr>
        <th> Name </th>
        <th> Action </th>
      </tr>
      <% @products.each do |c| %>
        <tr>
          <td> <%= c.name %> </td>
          <td>
              <%= link_to "Edit", admin_edit_product_path(c) %>
              <%= link_to "Delete", admin_product_path(c), method: :delete %>
          </td>
        </tr>
      <% end %>
    </table>


  </div>
</div>
